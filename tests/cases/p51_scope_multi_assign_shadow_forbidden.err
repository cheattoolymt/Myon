# Expect a non-zero exit: multiple assignment inside a `{ }` block that
# targets outer-scope variable names is forbidden shadowing (spec 9.2).
# Only the non-zero exit is pinned, not the exact message text.
